name = "exam-study-sync-proxy"
main = "worker.js"
compatibility_date = "2026-08-12"

# 部署时自动在 980923.xyz zone 下创建 sync 子域 route + DNS 记录（橙色云朵 proxy）。
[[routes]]
pattern = "sync.980923.xyz/*"
zone_name = "980923.xyz"
